# config.local — Machine-specific git settings (not tracked in git)
# Copy this file to config.local and fill in your details.

[user]
    name = Your Name
    email = your@email.com
    # signingkey = ~/.ssh/id_ed25519.pub

# [commit]
#     gpgsign = true

# [gpg]
#     format = ssh

# [gpg "ssh"]
#     allowedSignersFile = ~/.config/git/allowed_signatures
